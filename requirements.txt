# Core dependencies
python-dotenv

# LangChain
langchain==0.3.3 
langchain-openai
langchain-community==0.3.1
langchain-core
langchain-chroma

# OpenAI
openai

# Vector Store & Embeddings
chromadb
# Tokenization
tiktoken

# Document Processing
docx2txt
pypdf
openpyxl
python-pptx  # ✅ Use this — 0.5.2 is outdated and causes build errors on Windows
python-docx
docx
pyPDF2
unstructured
# Data Processing
pandas
numpy

# Pydantic for structured outputs
pydantic
pydantic-core

# Streamlit for web app
streamlit     # ✅ Stable with latest numpy/pandas versions

# Visualization
plotly        # ✅ Compatible with streamlit 1.34
matplotlib

# Async support
aiohttp
asyncio        # Still fine, though asyncio is part of stdlib — can be removed if unnecessary
langchain-text-splitters
